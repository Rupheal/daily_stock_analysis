name: DSA Daily U Formal Producer

on:
  push:
    branches: [fix/native-private-execution-20260914]
    paths:
      - docs/runtime/DSA_DAILY_FORMAL_TRIGGER.json
  workflow_dispatch:
    inputs:
      target_session:
        description: XHKG target session YYYY-MM-DD
        required: true
        type: string
      dry_run:
        description: Preflight only
        required: false
        default: true
        type: boolean

permissions:
  contents: write
  actions: read

concurrency:
  group: dsa-native-private-branch-writer
  cancel-in-progress: false
  queue: max

jobs:
  produce:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    env:
      TZ: Asia/Hong_Kong
      PYTHONUNBUFFERED: '1'
      REAL_ORDERS_AUTHORIZED: 'false'
      PYTHONPATH: ${{ github.workspace }}/research:${{ github.workspace }}/research/scripts
      DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
      DSA_DRIVE_FOLDER_ID: ${{ secrets.DSA_DRIVE_FOLDER_ID }}
      DSA_DRIVE_CLIENT_ID: ${{ secrets.DSA_DRIVE_CLIENT_ID }}
      DSA_DRIVE_CLIENT_SECRET: ${{ secrets.DSA_DRIVE_CLIENT_SECRET }}
      DSA_DRIVE_REFRESH_TOKEN: ${{ secrets.DSA_DRIVE_REFRESH_TOKEN }}
      DSA_DEEPSEEK_V41_TOKENIZER: ${{ github.workspace }}/deepseek-recipe/static/tokenizers/v41/tokenizer.json
    steps:
      - uses: actions/checkout@v5
        with:
          ref: fix/native-private-execution-20260914
          path: research
          fetch-depth: 0
      - uses: actions/checkout@v5
        with:
          repository: deepseek-ai/deepseek-recipe
          ref: 8cadfede7063c896b944e7bae05daa3549ae97ea
          path: deepseek-recipe
          sparse-checkout: static/tokenizers/v41/tokenizer.json
          sparse-checkout-cone-mode: false
          persist-credentials: false
      - uses: actions/setup-python@v6
        with: {python-version: '3.11'}
      - name: Install bounded runtime
        run: pip install -r research/requirements.txt pytest openpyxl requests httpx exchange-calendars yfinance deepseek-recipe==0.1.1
      - name: Resolve trigger
        id: trigger
        working-directory: research
        env:
          EVENT_NAME: ${{ github.event_name }}
          INPUT_TARGET: ${{ inputs.target_session }}
          INPUT_DRY: ${{ inputs.dry_run }}
        run: |
          python - <<'PY'
          import json,os,subprocess,sys
          from pathlib import Path
          if os.environ['EVENT_NAME']=='push':
              t=json.load(open('docs/runtime/DSA_DAILY_FORMAL_TRIGGER.json'))
              target=t['target_session'];dry=bool(t.get('dry_run',False));enabled=bool(t.get('U_enabled',True))
          else:
              target=os.environ['INPUT_TARGET'];dry=str(os.environ.get('INPUT_DRY','true')).lower()=='true';enabled=True
              t={'schema_version':1,'arm_token':'DSA_DAILY_FORMAL_v1','armed':True,'target_session':target,'nonce':'manual-'+target}
          Path('/tmp/trigger.json').write_text(json.dumps(t))
          if enabled:
              subprocess.check_call([sys.executable,'scripts/dsa_daily_formal_trigger_v1.py','--trigger','/tmp/trigger.json','--out','/tmp/trigger-validated.json'])
          with open(os.environ['GITHUB_OUTPUT'],'a') as f:
              f.write('target='+target+'\n');f.write('dry='+str(dry).lower()+'\n');f.write('enabled='+str(enabled).lower()+'\n')
          PY
      - name: Build current official snapshot
        if: steps.trigger.outputs.enabled == 'true'
        working-directory: research
        run: python scripts/dsa_daily_market_snapshot_v1.py --session "${{ steps.trigger.outputs.target }}" --u-universe docs/dsa-u-universe.json --out /tmp/daily-snapshot
      - name: Run U daily producer
        if: steps.trigger.outputs.enabled == 'true'
        working-directory: research
        run: |
          DRY=""
          if [ "${{ steps.trigger.outputs.dry }}" = "true" ]; then DRY="--dry-run"; fi
          python scripts/dsa_daily_u_production_v1.py --research-root . --target-session "${{ steps.trigger.outputs.target }}" --snapshot-dir /tmp/daily-snapshot --macro docs/runtime/U_DAILY_MACRO_AUTHORITY_LATEST.json --out /tmp/daily-u $DRY
      - name: Persist sanitized U status/formal receipt
        if: always() && steps.trigger.outputs.enabled == 'true'
        working-directory: research
        run: |
          set -euo pipefail
          test -f /tmp/daily-u/STATUS.json || exit 1
          cp /tmp/daily-u/STATUS.json docs/runtime/U_DAILY_GENERATION_STATUS_LATEST.json
          python - <<'PY'
          import json,shutil
          from pathlib import Path
          p=Path('/tmp/daily-u/formal/SANITIZED_U_FORMAL_RESULT.json')
          if p.exists():
              d=json.load(open(p))
              if str(d.get('state','')).startswith('PASS_FORMAL_U_DECISION') and int(d.get('formal_valid_rows',0))>0:
                  shutil.copy(p,'docs/runtime/U_PRODUCTION_FORMAL_LATEST.json')
          PY
          git add docs/runtime/U_DAILY_GENERATION_STATUS_LATEST.json docs/runtime/U_PRODUCTION_FORMAL_LATEST.json 2>/dev/null || true
          git config user.name 'dsa-daily-u'
          git config user.email 'dsa-daily-u@users.noreply.github.com'
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "dsa: daily U formal status ${{ steps.trigger.outputs.target }}"
          git pull --rebase origin fix/native-private-execution-20260914
          git push origin HEAD:fix/native-private-execution-20260914
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: DSA_DAILY_U_FORMAL_${{ github.run_id }}
          path: /tmp/daily-u/
          if-no-files-found: warn
          retention-days: 30
