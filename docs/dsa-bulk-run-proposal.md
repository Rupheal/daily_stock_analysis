# 全池模型运行方案与费用范围（尚未授权执行）

本批已授权的两次单股HTTP中使用1次，另1次保留；该额度不包含全池。已完成U45和原版O660的免费原生数据运行及失败台账，没有通过全池模型、新闻或策略门槛。

## 具体执行范围

U固定45只，资格失败或缺证据继续保留分母，以watch/不可执行状态留档。O使用每个决策截止已生效的沪深可买卖股票并集，当前完整交易日2026-09-11的N=660。9月14日调码须在下一轮名单核验后生效，不能跨日继续把660作为常数。

先免费处理原版00719、01318、06715异常OHLC，保留原生字段与外部对照，不替换原版核心逻辑；为所有准备调用模型的股票准备数据与时点清单。若个股失败则不发送其收费请求，记录未覆盖，不能将657改为O分母。两版隔离数据库/输出；原生O保持089d9d26及冻结配置，U保持对应修复提交。排名只展示原生可比评分，不自行补分或把样本排名写作全池Top3。

指定API Models的O账号/模型/原生请求单价未验证，因此该路线的完整费用仍未知；不能拿已有DeepSeek余额冒充API Models账号已就绪。须先明确拟使用的模型、是否有原生已有配置支持、密钥/权限实际可用性及每次HTTP计数方案，再把最终批准放在发送批量请求之前。

## 已核实价格下的可审阅替代场景

如果用户另行同意让O也通过原生已有配置使用现有官方DeepSeek Flash，以下仅为该替代场景的预算。不会因为写入方案而自动切换O提供商。

[官方价格](https://api-docs.deepseek.com/zh-cn/quick_start/pricing/)核对于2026-09-12：空闲时段每百万token未命中输入1元、输出4元；高峰2元、8元。实际已验收单股为12406输入、4418输出（包含思考token），空闲估算0.030078元、高峰0.060156元。

| 场景 | 至多HTTP数 | 按这次单股规模空闲估算 | 同规模高峰估算 | 每请求预留0.40元的保守额度 |
|---|---:|---:|---:|---:|
| U45一轮 | 45 | 1.353510元 | 2.707020元 | 18元 |
| O660一轮，替代提供商场景 | 660 | 19.851480元 | 39.702960元 | 264元 |
| 两版各一轮 | 705 | 21.204990元 | 42.409980元 | 282元 |

这些是测量样本外推，原版提示词/输出可能更大，不能视为报价或实际扣款。开始前逐请求限制原生可配置输出上界、请求体大小和总HTTP数；超过边界先拒绝该请求，不截断关键证据。收费重试及备用模型为0，原生逻辑若必须额外HTTP则先计入方案，不能藏在一次“运行”内。计价模型/地址改变时旧价目不适用。

优先按两阶段批准：先确认O提供商及可复现单股配置，再针对完整批次批准明确HTTP数和金额上限。现有1元单股目标和1.20元申请阈值不被本表扩大。余额最近返回9.77元，实际扣账仍未核实；是否充值要按真实提供商账户余额和最终上限评估。API Models无可核实报价时不填0元。

不增加订阅，不自动购买数据，不启动全池收费或定时任务。GitHub运行器实际账单未知，“免费数据验收”指没有模型API/付费数据请求，不代表平台基础设施一定零费用。

## 暂不增加费用时的可用方案

继续交付已有小米事实+受约束观察，保留U45/O660数据台账，免费维护证据和前向观察。全池模型排名与策略胜出结论保持未完成。资料缺少历史时点或未来窗口未成熟时，扩大模型调用次数不会解决这类缺口。
