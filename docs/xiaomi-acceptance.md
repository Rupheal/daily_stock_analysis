# Xiaomi single-stock acceptance

Manual workflow `Xiaomi original DSA single-stock acceptance` runs upstream commit `089d9d26d68f8b839ea5a74a3784e4402925f8b7` unchanged for `hk01810`, using the official DeepSeek API and repository secret `DEEPSEEK_API_KEY`. No notification or market review. The model alias is `deepseek-flash`; record returned model/version and usage after execution. Native reports and the fresh usage database are retained for 7 days. A 600-second process timeout limits runtime, not exact billed cost. No automatic rerun is configured.

本次仅为用户授权的小米单股验收，不代表全市场排名或策略有效性。API余额由用户报告为人民币10元，尚未独立核实扣费。报告与数据库为此公开仓库的运行产物，不含密钥配置。行情/新闻缺口及模型失败必须单列，不以工作流绿色代替报告验收。回滚：停用或删除这条新增手动工作流，不修改原版策略。
