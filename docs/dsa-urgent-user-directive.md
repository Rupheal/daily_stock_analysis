# 用户最新执行指令：充值后优先完成周一开盘前交付

## 2026-09-17 Run033—040 已验证接续入口

当前证据以 `docs/runtime/RUN033_040_RESUME_RESULT.json` 与 `docs/DSA_NEXT_ACTIONS.json.current_execution` 为准。O 当期分母660，598只已通过冻结原版数据 CLI 实际消费，62只隔离；U45已逐项审阅、44只行情事实可复用，正式信号仍0/45。两次新原生请求均保留失败，不能把错误兜底当成模型意见。已封存的腾讯、快手 Claim 不自动重复。原始内容保存改为私密分包，调用前必须先通过私有保存/读回。Run040 对截断回答、空正式回答、success=false和错误消息加硬性拒绝，104项云端回归通过。

模拟运行授权继续读取唯一 RUNTIME_STATE；已批准、已激活，不再申请相同窗口。下一阶段先处理原版模型配置/输出预算兼容性、62只输入隔离和U正式信号生产器；只允许有版本、可追溯、通过自身门槛的新信号进入未来窗口。本轮未部署每日全池模型生产任务，不可声称自动出票闭环已完成。

## 2026-09-17 最新接续：正式双轨验收，不重复请求既有窗口批准

用户最新明确要求：完成 O 原生全池出票、U 逐股验收，再用真正合格的信号验证模拟成交；立即连续执行。
- 当前授权/激活读取 TRIDENT runtime 分支 `control_room/dsa/runtime/RUNTIME_STATE.json`。已经批准的窗口不因历史文档中的 false 字段再次冻结或重新索取批准。
- 已关闭的 Gate A 批次 007 不再调用，也不借用其剩余名额。其原始失败与版本化发布视图 PASS 均保留。
- Run033 免费保存缺失的全池历史输入原件并逐股独立核验；Run032 已通过的四股修复及公告证据复用。
- 必要的下一阶段原生样本使用新的 Run/Claim，先价格与证据预检、具体请求费用预演、当前余额和私有保存检查。Run034 仅对已有复核公告的两个新标的测试通用入口，每股最多一次、每股上限 CNY0.10、合计 CNY0.20，受用户既有必要 DSA 研究余额授权覆盖；不复用小米历史调用。
- 原生全池分母不变；少量可验样本不等于全池排名。U 独立准备继续；技术排名不能替代带完整证据与交易规则的 U 正式信号。
- 原始模型内容只在已验证 owner-only Drive 保存并独立读回；公开仓库只记录程序、公开数据及脱敏验收信息。账户余额详情不公开。
- 只接受未来及时冻结的合格信号进入已批准模拟窗口；今天过期的上午窗口不补单。main、Production、真实订单、定时时点均不改变。

以下为历史授权记录；发生冲突时以上述当前范围与用户最新明确指令为准。

## 2026-09-16 最新用户覆盖：关键 Gate A 批次授权（最高优先级）

用户最新明确指令：
“为了推进关键进程，可以申请5次Deepseek请求+ ¥1.0硬上限，请更新指令”

本节覆盖本文件中与 O Gate A 关键推进冲突的旧单次请求/小额预算限制，但不改写任何历史 Run、历史费用或历史失败结论。

执行规则：
- 新建独立批次授权：`TRI-DSA-AUTH-20260916-O-GATEA-CRITICAL-007`，正式回执见 `docs/runtime/O_GATEA_CRITICAL_BATCH_AUTH_007.json`。
- 在 O 单股 Gate A 关键推进范围内，最多允许 **5 次新的 DeepSeek provider HTTP 请求**，批次累计硬上限 **CNY 1.00**。
- 这是“最多可用”的上限，不是要求用完。Gate A 一旦真正 PASS，立即停止该批次后续付费请求；不得为了消耗额度继续调用。
- 每次付费请求必须建立新的独立 Claim；Run030 及任何失败 Claim 不得重用、重解释或覆盖。
- 每次请求前必须 fresh target-session + strict preflight + 价格完整性 + 请求成本预演；累计实际/保守计费不得超过 CNY 1.00。
- 每次失败后先做 model-free 诊断、修复与回归验证，再决定是否动用下一次请求；禁止无诊断连续重试。
- 同一根因若连续两个付费样本仍不收敛，暂停继续付费，先做架构复核或更换可验证方案，不机械消耗剩余请求。
- 禁止 fallback model、自动重试、自动充值、新订阅、付费数据扩张和无关模型实验。
- 私有原始证据继续保存到 Google Drive owner-only 区域；必须 save/read/hash/restore 验证。公开 GitHub 只允许代码、脱敏回执和必要哈希，不得公开原始 Prompt/Response。
- Gate A 通过之前：Gate B、Gate C、O 全池、U45 正式执行、模拟交易、runtime 激活继续冻结。
- 即使 Gate A 通过，剩余额度也不自动等价于 Gate B/C 或 O 全池的新权限；按既有门控规则进入下一阶段。
- 不修改 main、不修改现有定时任务、不修改历史预测或权威模拟账、不产生真实订单。

本批次计数起点：新批次 HTTP 请求 0/5；新批次模型成本 CNY 0.00/1.00。Run030 已经消耗的历史请求和成本保持历史记录，不重复计入本批次，也不清零。

## 2026-09-14 最新用户覆盖：Drive 优先，U 准备独立推进

用户已明确要求立即执行以下调整；本节覆盖旧的存储优先级和 U 准备串行限制。
- Google Drive 为原始证据首选保存位置；使用已有 TRIDENT_BACKUP 下的 DSA_PRIVATE_EVIDENCE。目标文件夹已经连接器元数据核验为未共享、仅 owner 权限；具体 ID 保留在私密配置中，不写公开仓库。
- GitHub 保留代码和脱敏验收摘要；Run011/Run012 已有私密 GitHub 有界文本保存工程及证据完整保留，降为备选。DSA_EVIDENCE_TOKEN 不再是 Drive 主路径的必需配置，不要求 Owner 为首选方案先配置它。
- Drive 连接器可访问文件夹不等于 GitHub runner 已获得 Drive 授权。必须验证 runner 身份、最小访问权限、原始字节上传/读取/SHA-256 一致性；ZIP/JSON 等保留原格式。连接器探针不能代替 runner 验收。
- 禁止经公开 GitHub 日志或附件中转原始模型内容；不导出连接器凭据或模型密钥，不在聊天索取密钥。
- O 存储等待期间，U45 的身份、当期沪深港股通并集买入资格、停牌、复权、量单位、新闻公告、资金数据和时间戳核验独立推进；复用 Run009 收据，失败保留在45分母中，不重复全池抓取。
- 此次解除的仅是 U 独立准备等待 O 的限制；正式模型验收、合格 BUY、完整费用/汇率/整手及发布后价格等信号和模拟门槛不变。
- NAS 仅作未来第二份独立备份，目前未接入，不作为前置条件。不新增订阅或购买服务。
- 日常简报继续报告实际状态；不修改定时任务、main、历史预测或权威模拟账。
- 外部配置不足时立即给出具体缺口及下一动作，不重复同一检查空等；常规独立准备继续推进。

本次调整是执行政策与接续清单更新，不代表 Drive runner 保存、O 原生报告或 U 正式验收通过。


用户原话：
“通知隔壁，我充值了，不用节约预算，争取加油持续完成工作。我拖不起了，下周一马上开盘了。”

本指令面向同项目“完善双模型简报”执行工作区。用户已提供DeepSeek充值账户截图；截图及账户余额不上传公开仓库。

执行含义：
- 用户已追加必要DeepSeek研究调用的费用授权，不再因此前小额预算（包括旧两次单股限额或后续8元预算）反复停下等待确认。保留历史费用和真实HTTP计数，不清零。
- 以2026-09-14周一港股开盘前的可审阅产物为近期优先交付目标：先完成双轨运行和独立覆盖，给出可用候选、证据及失败项；不能把单股或部分池排名冒充全池TOP3。
- 接续已运行任务及已验证断点，避免重复启动、重复付费。同一问题不收敛时隔离并保留问题，继续其他可做工作。
- 必要模型调用按实际余额、当前价格、请求上界及已预留费用执行。不是要求用尽余额；不扩张到无关模型实验、新订阅、付费数据或自动充值。
- 不因赶开盘而伪造结果、跳过关键数据校验或宣称时间外有效性已成熟。持续交付可用部分，历史PIT和未来窗口缺口单列。
- 不修改定时、不合并main、不对外发布或下单。O/U原有独立股池和原生逻辑要求保持。

送达方式：通过共用修复分支及DSA_NEXT_ACTIONS.json提示。此记录不等于另一聊天已读回执，也不自动唤醒该聊天。
