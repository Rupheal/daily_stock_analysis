# DeepSeek 8元研究预算与原版测量

用户最新goal直接授权本目标所需DeepSeek费用累计不超过8元，无须逐次询问。该授权取代旧的两次单股限制，不授权新订阅、其他付费数据、通知、定时发布或交易。

本批先使用原生上游089d9d26的实际小米调用测量提示词/输出；利用现有OpenAI-compatible渠道配置DeepSeek。用户已明确允许本目标的DeepSeek调用，不能再以旧API Models账号条件阻塞。上游代码、提示词、评分、原始答案均保持；外围只校验已有独立日线、观察HTTP用量并限制收费次数。

`scripts/run_hk_original_model_probe.py` 复用免费小米预检，执行原生main入口。12工作流当前仅允许本具体push的一次请求；不允许重跑/手动dispatch再计费。预算接纳前原子落账；失败或无usage保留预留额；stream原始响应保存，不改变请求内容。文本请求限制100KB，原生输出上限8192tokens，按150k输入估计和高峰费率预留至多0.365536元。包括思考tokens的输出只计一次；预算是估算控制，不是账户账单。

保守结转上一研究请求高峰估算0.060156元；历史原始费用单独保留，未核实的整项目历史扣费不称为已核清。本次预算硬上限为8元，不沿用旧20%超额余量。

完整O660加U45按已测受约束单股规模的空闲估算约21.204990元。此数不是原版报价。先测量真实O规模，再评估8元内可完成的范围；不把原版压缩成多股一次问答，不改原版逻辑来制造低价全池结果。任何未覆盖股票仍保留分母，样本分析不称全池TOP3。

原版结果仅进入隔离诊断产物，人工核验之前不签发交易结论。回滚为关闭12工作流；固定事实V1、主main、Web/API/Agent与通知入口不受本脚本影响。本中文专项说明无对应英文版。
