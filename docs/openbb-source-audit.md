# 小米免费数据验收

在独立修复分支运行 `04-xiaomi-openbb-probe.yml`，OpenBB 使用隔离环境，DSA 依赖保持独立。脚本 `scripts/probe_xiaomi_sources.py` 保存行情、历史、新闻的原始响应及 UTC 采集时间。所有请求为读取；未配置模型密钥。单个 OpenBB 请求最多 90 秒，其他请求 20 秒超时。接口成功不代表数据验收通过，必须核对日期、复权、单位、实体关联和原文。结果存储在 Actions artifact，保留七天。

原版 main 不变；无定时任务。回滚可删除本工作流和探测脚本，不影响模型主流程。

此文为当前项目专用中文验收说明，无对应英文文档。

## 已执行结果与单股验收

源探测 run 34641012836 成功。OpenBB/Yahoo 与腾讯 70 个重叠交易日 OHLC 在 0.005 HKD 容差内一致，volume 完全一致。最新交易日2026-09-11，收26.36。Yahoo quote 的成交量比日线多600股，且无行情时间戳，故只采用交叉核验日线。腾讯/新浪快照是16:09，不能单独证明最终日线；本轮以两条日线独立一致作为完整日线验收依据，不宣称交易所官方核证。

原生8源197条中港股查询无小米匹配。OpenBB公司新闻10条，人工复核后选择2条，见 `xiaomi-reviewed-news.json`。这是本轮有期限的人工核验输入，非通用自动新闻适配器：Reuters原始报道9月9日、Yahoo转载9月10日，内容是建议调查而非裁决；欧洲计划9月3日宣布，9月9日报道是后续，不是新催化。公开检索摘录及摘要已核对；完整正文受429/403或JS影响未取得，不能声称全文或全部披露覆盖。小米IR RSS最新2022年，排除。

`05-xiaomi-verified-acceptance.yml` 下载指定源探测artifact，`prepare_xiaomi_acceptance.py` 检查快照≤24小时、目标交易日、至少60天独立重叠、OHLC/volume、新闻≤3天及原始发现ID，然后导入同一job的全新DB。使用原有日线清洗/指标/存储与新闻池服务。此一次性验收使用可追溯已核验快照，非每日自动获取；未修改原版main，未启动定时或TOP3。数据失败不会进入带密钥步骤；报告字段补全重试为0。只运行一次单股任务，网络层原有重试仍保留。余额差仅为期间账户变化，不能排除其他并发消费。

49项相关测试、本地实证预检、py_compile、diff和YAML检查通过；未运行完整后端CI。新增腾讯港股路径纳入能力过滤；默认优先级不变，旧Yahoo返回过期数据仍会被完整日线门槛阻断。本轮workflow使用已有TENCENT_PRIORITY=0配置，无新增配置项。回滚本次提交可撤回该入口和新增支持；之前完整日线保护继续保留。

首次输入验收run 34642001260：49项测试和数据预检通过，但周六交易日检查跳过主程序；没有模型调用，前后余额均CNY9.96。手动入口补上原有`--force-run`，仅允许休市日执行，不关闭数据完整性校验，亦不强制刷新已核验日线缓存。

## 最终输出复核（2026-09-12 香港时间）

run 34642291178 在 commit 171e40243be0568a936113b1b380e0f03c546d86 技术运行成功，49项云端测试通过，实际完成一次 deepseek-flash 分析，原始结论观望/45分（不是胜率），报告已存artifact。**技术成功不等于内容完全合格**。

实证输出复核未通过：①volume_ratio填0.76，实际前五日均量口径0.72，0.76仅为较昨日倍数；②实体较小错误，实体0.70占振幅1.22的57.38%；③上下影线均较长错误，上影0.30、下影0.22，均小于实体；④25.00止损相对26.36收盘距离5.159%，不足以支持无统一入场价的5%以内承诺。人工还发现笼统均线下行表述需修正：MA20从27.35升至27.39；买入条件散落多处，尚未成为统一执行规则。因此不发布原始执行计划为已验收建议。

新增`audit_daily_report`及单股验收出口门槛，用真实输出离线复放成功发现上述4项；51项相关本地测试通过，未增加模型调用。门槛仅接入单股验收脚本，非所有API/Agent/定时出口；不宣称全面自然语言验证或全后端CI通过。自动产生的原始报告仍保留供复盘，但不可误标为合格交易计划。修订入口的上下文审计提取真实`enhanced_context`层。

费用：账户CNY9.96→9.96，USD0→0，余额接口显示精度内未见变化，不表示零成本；llm_usage仅1行analysis，prompt/completion/total均0，但reasoning_tokens=4004、cached_tokens=2688，遥测矛盾，精确费用未核实。未新增订阅或付费数据源。保留原版main=1c2b7642f594714be9f0c4d73a08cc492ccd0813；TOP3与定时任务未启动。

复权：OpenBB请求splits_and_dividends；腾讯请求qfq，返回day字段。对齐窗口70日日线数值一致，不能外推为所有公司行动、所有历史区间口径永久一致。最新日线是2026-09-11；快照采集UTC2026-09-11T19:51左右，不能冒充实时推送。
