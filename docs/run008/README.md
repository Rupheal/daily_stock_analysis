# Run008 新时点恢复数据配对研究

Run ID: TRI-DSA-VAL-20260914-008。父节点6ac18ddc；实验不替换Run005 A0/A3，不改Main/任务/Shadow。
当前官方两通道691证券代码集合一致，剔除31 ETF，股票并集660。9月14日调出01196、调入02922；不擅自延续其旧代码价格。
输入为Run006已验收的9月11日完整交易日特征，新增真实模型调用前冻结输入时点。只允许9月14日港股收市前完成，避免把9月11日当成收市后的最新日线。
研究参考入场最早为9月15日09:30实际可验证价格；绝不获取未来结果，也不触发30万元账户。模型身份是既有technical scorer，不是原版native DSA。
A0原提示与参数保持，A3仅删除realized_vol20_ann_pct。同池各自全量尝试，23隔离保留（原22加新代码历史待核）；模型失败额外增加隔离。
费用按旧高峰3/9元每百万token保守计提，高于9月14日官方Flash高峰2/8；不使用缓存折扣降低预算，实际发票未知。最高8元且不超过当时核实的CNY余额，不充值。
源码接受8项独立预检。生产入口无dry-run假分数；只有真实模型四个受限字段/用量/hash保存，不输出raw提示/响应/私密余额。
任务中断保留已完成请求检查点；本次GitHub run不允许重跑。恢复需要新Run并排除已发出的请求，不能为了改善结果重试。
Control Room的Checkpoint05为4/11旧快照；更新NEXT_ACTIONS及V4/V7收据给出6/11。本实验不增加Run005已验收分子。V2/V9 WAIT；V8单状态；V11 NOT_ELIGIBLE。
