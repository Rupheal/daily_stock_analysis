# 港股免费新闻与日线接入

修复分支专用，main 保留。富途行情 API 暂缓，允许15分钟行情延迟，分析仍使用完整交易日日线。

## 2026-09-12 接口实测

云端只读测试 run 34644529504：富途公开页返回公司新闻及 epoch 发布时间；新浪公司页返回文章链接；新浪搜索和财联社搜索只有动态网页框架；东方财富搜索返回错误栏目/无关公司，日线连接中断。这些失败不作为“无新闻”的证明，也不触发付费订阅。

新增公开新闻适配器读取富途结构化网页数据和新浪文章明确时间，沿用原生 DNS/SSRF/大小限制。新闻需通过公司关联、发布时间、三日时效和原文链接检查后入原生资讯池，并进入 DSA 上下文。保留媒体与获取渠道，不把富途转载财联社算成两个独立信源。只去重 URL 和标准化标题，跨语言、同事件不同标题仍可能重复。公告事实必须另核发行人或港交所；摘要、媒体报道、分析师观点均不能宣称原文事实已经全部核实。

港股日线顺序改为腾讯、Akshare（东方财富）、Yahoo及其他后备；不改变A股顺序。按显式 end_date、冻结任务日期或港股交易日历确定目标；缺目标日线时继续切换。停牌缺日线不自动补造。

报告确定性复核接入 classic 和 Agent 港股分析保存历史之前；复核失败清除执行仪表盘并标记失败，阻断历史交易信号与正常通知。该检查覆盖已观察到的数值/语义错误，不等于所有自然语言事实都已验证，也不涵盖绕过 pipeline 的直接分析器调用。

新增07手动/修复分支路径触发验收，不注入模型密钥，不新增模型调用。05旧模型验收工作流本轮未触发。TOP3和定时任务不启用。

## 验收结果

- 只读发现：run 34644529504。
- 首轮接入：run 34645242906，55项测试；富途11条通过，新浪旧编码失败。
- 编码修复后：run 34645447181，commit 5b3e2cd4dc13269d0f74304f10685a9988e377b7，55项云端测试；富途11条、新浪6条共17条入库并进入DSA；腾讯返回138根日线，最新2026-09-11，OHLC=25.66/26.66/25.44/26.36，volume=113533443，MA5/10/20=26.62/27.22/27.39，五日均量比0.72。
- 最后增加UTC发布时间窗口（排除未来新闻）与金融相关性排序，避免教程挤掉监管/资金信息；用上述真实17条新闻离线入库并调用实际DSA消费者，验证UTC标签、印度风险和财联社资金报道进入前6条，排除键盘教程。最终本地94项相关测试通过；最后的时区/排序收尾未再次跑云端，未执行完整后端CI。
- 上一轮真实模型输出离线回放仍检出4项错误，禁止将旧执行计划冒充验收通过。本轮没有再次运行模型，故不宣称“使用新17条新闻生成的模型报告已经通过”。

## 剩余边界与费用

东方财富当前云端访问异常仍存在，作为后备源保留；财联社直连搜索尚无静态结果，已通过富途获取其报道；港交所发行人公告自动直连、全文事实逐条核验、跨语言事件去重尚未完成。新闻窗口和数量受公开页首屏限制，不是全网新闻穷尽。当前新闻分类/排序是透明关键词规则，非投资重要性的完整判定。

本轮新增LLM调用0，不新增数据订阅。GitHub Actions实际计费未核实，不宣称所有运行零费用。上一轮DeepSeek余额显示CNY9.96→9.96但token遥测矛盾，精确模型费用未核实。

main保持1c2b7642f594714be9f0c4d73a08cc492ccd0813。仅修复分支持有更改；TOP3与09:00/16:30定时任务未启用。下一阶段应先验收新新闻驱动的模型输出和统一入场/仓位/止损规则，再启动双模型比较与定时扫描。
