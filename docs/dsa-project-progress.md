# DSA项目进度与后续接续

核对日期：2026-09-12。可用版代码：`d9edcb59938f97e68e6a8a2d3289c5f0806717dd`；父提交：`f8fc7f3b45a9053f04e3e215a8c77d79ea9a4b12`。main仍为`1c2b7642f594714be9f0c4d73a08cc492ccd0813`。

## 当前交付

小米固定事实简报V1已经实现并完成云端运行：[34700162747](https://github.com/Rupheal/daily_stock_analysis/actions/runs/34700162747)。244项测试通过、7项警告、14.79秒；其中新增14项全部在云端执行，本地13项执行、1项因依赖不全交由云端。回放复用仓库保存的5份真实失败模型输出，旧叙述不能进入事实产品。

数据预检：2026-09-12T14:46:32.269774+00:00（北京时间22:46:32）。最新完整日线为2026-09-11；腾讯/Yahoo核对126个共同交易日。6篇核心公司新闻、4家原媒体、5个候选事件、2项未解决风险、4条近期公告目录。程序直接生成HTML、Markdown、JSON及manifest；工作流摘要显示Markdown，完整原始采集文件在运行artifact中。本次下载交付的文档与预检取自该运行的日志JSON，并由同一代码渲染；没有将无法本地下载的远端ZIP称为已独立验签。

事实版只包含固定计算、新闻标题索引、来源和持续风险状态，不包含模型预测、仓位或买卖指令。直接使用方式见[xiaomi-fact-brief.md](xiaomi-fact-brief.md)。新入口已通过实际运行，既有Web/Agent/通知的自由模型报告入口未被替换，不应称这些界面也已交付事实模式。

HTML通过内容结构、链接和外部文本转义检查；当前本地缺浏览器可执行文件，未完成浏览器像素渲染检查。未创建PR，临时截图未入库。此前原始模型报告的语义批准状态保持未通过。

## 进度按交付阶段分开

| 阶段 | 状态 | 完成标准/剩余工作 |
|---|---|---|
| G1 固定事实产品 | 完成 | 无模型入口、最终文件、失败出口及真实云端数据验收已通过 |
| G2 支持分析的数据证据 | 部分完成 | 日线和境外新闻已接通；公告全文、实际使用财务字段、事件聚合泛化仍需补证 |
| G3 受约束的模型单股报告 | 部分完成 | 统一执行字段及风险拦截已有；尚无全部语义批准的原始报告 |
| G4 原版O/升级版U独立运行与股池覆盖 | 未验收 | 规则和股池协议已确认；全池原生运行、覆盖台账及最终展示仍须实测 |
| G5 资金潮汐/宏观与历史对照 | 未验收 | 有研究规则与历史回放要求，未核实完整可复现结果；不能宣称已校准 |
| G6 发布准备与持续验证 | 未验收 | 运行可靠性、真实消费者、费用核对、影子记录与部署方案仍需完成 |

按上述6个阶段的“已关闭阶段数”计为1/6；这个计数不是工时加权的工程完成率，也不是模型准确率。启动下一条goal时，依照这些阶段将实际验收项冻结为明细清单，再按已完成项/总项报告；不得因删除困难任务扩大完成率。未来必须等待的观察期、需资源审批的工作单列，不能把时间未到算失败或把长期目标写成已完成。

## 范围依据

已重新读取现存双轨协议v1.4，文件身份`libfile_c7d0f996ba1c8191b72499ff57b4cd92`，版本4，修订2026-09-11T13:15:36.437789Z。最新规则覆盖历史的“共同23股”约定：U_POOL_2026-09-11_v3为45股，O_SC_ALL_v1为当期沪/深南向官方可买卖股票并集N。45股是用户候选成员记录，并非全体身份/资格已验收；如03308对应名称仍需官方核验，不静默删除。两套系统可以自然选出相同股票，但不共享筛选或改写原版逻辑。

宏观研究保留HK Capital Tide、Global Relay/Macro Overlay及人物言论冲击的既有要求。2025年4月至2026年9月历史事件回放和1/3/5/10/20交易日跟踪属于后续研究；不把美股PIT项目的Universe、$10B门槛或校准结果移植成本项目有效性证据。仅沿用来源哈希、真实运行计数、原子断点及各层验收分开的工作方法。

## 费用与权限

本次新增模型HTTP请求0次，模型API费用0元；没有新订阅。GitHub实际收费未核实。最近上一批3次模型调用的用量估算合计0.09728896元，实际扣款未核实，这不是整个项目历史总费用。

05付费模型入口继续关闭，剩余旧授权请求为0。`dsa-research-goal.txt`中的最多2次、目标1元/申请阈值1.20元属于建议的下一批授权；仅在用户实际提交或明确采纳该指令后生效。本次编写文本不触发调用。全池模型批量运行、新数据订阅及定时发布另有范围与费用评估，不继承为无限授权。

本次未修改任何定时任务、TOP3任务、通知或main。协议记载有既有09:00/16:30任务，但本次没有查询或改变其当前启用状态；不将“本次未启动”误写为“账户没有任何已存在的任务”。

## 接续和回滚

最新检查点为`xiaomi-acceptance-checkpoint.json`，后续队列为`DSA_NEXT_ACTIONS.json`。队列是可核对的任务说明，尚不是已部署的跨运行自动调度器。先做G2中实际用于报告的证据补齐；G1无需为尚未使用的完整Web/Agent或策略概率重新开启验收。

撤回可用版提交中新增报告模块/入口和07工作流改动可回滚。当前文档追加不改变程序运行。所有修改仅在独立修复分支，未合并main。本文是本WORK中文专项报告，无对应英文文件。
