# Run014｜恢复边界加固、原生调用防重复与U45恢复审计

Run ID：TRI-DSA-EXEC-20260914-014。父提交：618fbdd3a0ccc35d480469e1ce76580f498bece3；父代码7db7d23ee0f9301be67de2eee06a9043c9cda2a2，继续c71e019e的Drive优先政策。

## 本轮已执行

读取最新运行文档、完整NEXT_ACTIONS及Run010/012/013。通过Git blob哈希恢复待修改源码，不把当前环境没有的旧本地未提交工作树宣称已检查。旧“候选尚未提交”已被云端提交与Run013回执更新。Run008研究及Run009准备的性质未改变；旧原件未重新生成。

恢复5个合成失败案例并修补：解压总字节上限、超大manifest预检查、重复manifest路径、Windows盘符路径、缺失Drive version。新增流式读取上限及版本变更拒绝测试。它们不是已发生的原件损坏事件。

原生调用前新增持久化CLAIM：最终产物或CLAIM已存在都禁止自动重复模型调用；模型后保存失败及上传回执丢失必须先对账。原有Actions单写者并发组保留；不宣称跨未知写者或供应商端exactly-once。CLAIM不是模型报告，不自动删除，不可因换Run ID绕过对账。

43号先保存原始字节，不再依赖私密GitHub备选text-only转换；转换脚本与Run011/012备选桥保留。原生进程失败但运行器仍可执行后续步骤时尝试私密保存；硬终止无法保证临时原件存活，此时CLAIM仍阻止静默再次付费。冻结上游、提示词、评分、既有原生调用参数与armed=false未改变。Run014为工程审计，未执行原先保留的Run013单股调用槽位。

## 实测及保存

本地定向测试40项通过（18项原有＋22项新增）；Python编译和工作流内嵌Python语法通过。云端最终状态读 `docs/runtime/RUN014_CLOUD.json`，未出现实际回执前不记云端通过。

从原私密Drive恢复U45准备JSON与媒体ZIP。媒体包5个文件的manifest字节/哈希及3份RSS来源收据逐一回验。45/45身份收据一致、45/45来源时序一致；这不是45份Yahoo原始响应重新回验，本轮未取得其原始字节。将媒体线索的无时区显示时间关联原始RSS带时区发布时间、取得时间及保守可用时间，不改旧文件、不回填历史。

U45完整准备0/45；继承2026-09-14快照买入资格44/45，09618仍隔离。停牌、复权/公司行动、量单位、当前公告、资金、计划/费用均未补齐。保留45分母，逐行错误隔离；已有3股媒体线索不替代公告。未刷新市场快照、未产出模型分数、Top3或BUY。

## 资源与验收口径

本轮预算：新增模型请求0、付费数据0、新服务0。确定性Python执行恢复/测试，当前会话作复杂冲突审查，未另启动Work/Codex模型任务。DeepSeek请求0、tokens0、新增模型费用0 CNY；未重新读取余额，不能把历史余额写成当前余额。平台会话tokens、计费额度及Actions实际费用不可见。未事先登记可验证的耗时上限，不事后伪造预估；文件/工具回执时间用于实际审计。

固定历史清单逐项复算：12/16=75%；未通过G4.3、G5.2、G5.3、G5.4继续保留。工程测试不抬升模型/策略完成率。O本轮新增原生验收0、U正式验收0/45、模拟入场NOT_ELIGIBLE、策略有效性N/A。未成熟未来窗口N/A；不重解释Run005/Run008。

## 阻塞及下一动作

1. Owner按既有 `DRIVE_OWNER_SETUP.zh-CN.md` 完成仅drive.file的限定文件夹OAuth，并将四项DSA_DRIVE配置放入本仓库Actions Secrets；不在聊天贴凭据、不要求备用DSA_EVIDENCE_TOKEN。先44号runner保存/独立读回/恢复通过，再允许43号一次必要原生调用与人工语义复核。
2. 单股通过后重新冻结当期官方SSE/SZSE买入并集、核实余额并依据实测资源执行O全池；N660只是2026-09-11历史快照。全池及可比性通过后才排序，原生观察/卖出不改成BUY。
3. U独立准备可继续，正式验收顺序不变；后续需从原保存入口恢复Run009行情/官方原件，再补其余六类证据。
4. 本会话未发现可读写Library libfile及expected_current_version的接口；Drive未检索到权威Journal/Summary，不能视为丢失，也不能重建。无合格信号因此未写账、未复算虚构现时净值。出现合格信号前仍须恢复原ID最新版本并用既有CLI及CAS验收。

本轮简报：O WAIT、U WAIT、合格候选0、新增模拟成交0；该报告不冒充已写入权威账本的WAIT事件。没有修改定时任务、main、原始预测或账户初资。没有向公开仓库提交原始市场/模型内容、私人目录ID、账本或凭据。
