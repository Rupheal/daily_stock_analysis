# Run015｜Drive-first门槛复核、fallback解除、U45恢复边界与PIT/OOS冻结

Run ID：`TRI-DSA-EXEC-20260915-015`。
分支：`fix/native-private-execution-20260914`。
本轮以用户最新Run015指令覆盖更早的GitHub fallback原生调用arm；不修改main、不下真实订单、不改定时任务、不重写Run005/Run008预测。

## 一、恢复与冲突裁决

恢复Run014、RUN014_CLOUD、Storage Recheck、RUN013、当前workflow及分支HEAD后，发现仓库在Run014以后已经完成private GitHub fallback的正式runner布局验证，并在提交`220aa36beab640b762690e59364582d9b846bb66`把一次O-native fallback调用armed=true。

该arm与本轮Owner最新明确规则冲突：Run015要求44号Drive真实`SAVE + READBACK + HASH + RESTORE`全部PASS后，才能执行O单股原生调用。因此本轮没有利用更旧fallback授权启动模型，而是把`docs/runtime/run014-github-fallback-native-arm.json`改回`armed=false`，提交`9ef3dd1b0c44054ccca79fdf8fdf2d44538405b7`。private GitHub fallback保存/读回/hash及小米strict preflight的既有成功证据继续保留，只作为fallback工程证据，不等价于Drive acceptance。

## 二、Drive真实保存Gate

最新可验证storage-only recheck仍为`TRI-DSA-STORAGE-VERIFY-RUN014-R2`：四个DSA_DRIVE环境值在该runner任务中均不可见；真实保存、独立读取、SHA-256 roundtrip和磁盘逐文件恢复均未执行，storage stages为`0/4`，acceptance=`BLOCKED_CONFIGURATION_NOT_PASSED`。

缺失/不可见配置名：
- DSA_DRIVE_FOLDER_ID
- DSA_DRIVE_CLIENT_ID
- DSA_DRIVE_CLIENT_SECRET
- DSA_DRIVE_REFRESH_TOKEN

因此本轮严格保持：**Drive=BLOCKED；O native model call=0。**

连接的Google Drive账户可见`DSA_PRIVATE_EVIDENCE`文件夹，但当前连接器按标题及父目录搜索均未返回U45 preparation原件。这不能证明原件被删除或不存在；Run013/Run014已经有私密Drive保存及元数据回执。因此本轮禁止从聊天摘要或聚合统计重建45行冒充原件。

## 三、U45准备

继承并重新固化可验证聚合状态：
- denominator：45
- 已审阅：45/45
- identity receipt一致：45/45
- source clock order一致：45/45
- 2026-09-14历史买入资格正向：44/45
- 09618：保留U45成员，但买入资格隔离
- complete preparation：0/45
- formal native acceptance：0/45
- qualified signal：0

新增Run015恢复文件：
- `docs/runtime/U45_PREPARATION_STATUS.json`
- `docs/runtime/U45_EVIDENCE_GAPS.csv`
- `docs/runtime/U45_RECOVERY_MANIFEST.json`

这些文件只保存已验证聚合事实和缺口分类，不伪造不可读取的逐股私密原件。已知未闭合证据类别仍包括：当前交易/停牌、公司行动/复权、成交量单位/价格口径、官方公告、媒体到官方原件闭环、资金证据、公司计划/资本动作/费用事件、交易计划/费用输入。

因此本轮U45的Accepted Progress是**恢复边界和状态持久化**，不是把complete从0抬高；策略分数、Top3、BUY均未生成。

## 四、PIT/OOS与Ablation设计冻结

在不使用未来结果的前提下，本轮新增并冻结：
- `docs/runtime/DSA_PIT_OOS_RECORD_SCHEMA_v1.json`
- `docs/runtime/DSA_ABLATION_PLAN_v1.json`

PIT/OOS记录要求原预测append-only，`future_label_status`初始必须为`PENDING`；1/3/5/10/20香港交易日结果仅在成熟后追加，禁止覆盖原预测、事后改规则或删除WAIT/失败样本。

Ablation冻结A0为不可变Champion，A3_NO_VOLATILITY为独立Challenger；同时只定义未来NO_MACRO、NO_CAPITAL_TIDE等对照设计。没有足够真实OOS样本前，因子优劣结论保持N/A。

由于当前O单股/当前Universe尚未通过Drive-first Gate，本轮没有伪造一个新的A0/A3 pre-outcome预测时点。Run008保持冻结。

## 五、资源与Accepted Progress

新增DeepSeek模型请求：0。
新增DeepSeek tokens：0。
新增DeepSeek模型费用：0 CNY。
新增付费行情请求：0。
新增订阅/充值：0。
真实订单：0。
模拟成交：0。
权威Journal写入：0。
定时任务修改：0。
main merge：false。

本轮新增Accepted Progress：
1. 发现并裁决Run014以后出现的fallback arm与Run015 Drive-first新规则冲突；
2. 解除fallback arm，防止旧授权绕过新Gate；
3. 确认Drive最新真实验收仍为0/4，而不是把workflow green误报为storage PASS；
4. 固化U45聚合恢复状态、证据缺口与禁止伪造逐股原件的恢复边界；
5. 冻结正式PIT/OOS append-only记录格式；
6. 冻结A0/A3及未来Ablation治理设计。

工程与治理推进不抬升策略有效性。固定历史清单仍为`12/16=75%`；G4.3、G5.2、G5.3、G5.4继续保留未通过/未成熟状态。

## 六、本轮16项报告答案

1. O正式原生验收：**仍未开始；0个新增验收。Drive Gate阻断。**
2. U45完整准备：**0/45 complete；45/45 partial/reviewed；09618 eligibility isolated。**
3. U正式验收：**0/45。**
4. Drive：**NOT PASS；最新真实阶段0/4。**
5. O当前Universe：**尚未重新冻结；N660只能作为2026-09-11历史快照，不能冒充N_current。**
6. O全池：**未启动；success/fail/isolated当前均不适用。**
7. O Top3：**未正式可用。**
8. U Top3：**未正式可用。**
9. 合格模拟信号：**0；Simulation仍NOT_ELIGIBLE。**
10. 权威Journal：**本轮未写入，未新建替代Journal。**
11. A0/A3新pre-outcome冻结：**0；缺已接受当前O输入/Universe，拒绝伪造。**
12. PIT/OOS：**本轮新增真实预测样本0、成熟OOS结果0；记录schema已冻结。**
13. 模型资源：**DeepSeek请求0、tokens0、费用0 CNY；Actions/平台会话计费不可观测。**
14. Accepted Progress：见上文六项，均属于治理/恢复/PIT设计，不冒充策略表现。
15. 固定16项验收：**仍12/16=75%。**
16. 下一步最小自然触发：**Owner使四项现有DSA_DRIVE OAuth配置在repository Actions Secrets中可见，然后只跑44号；四阶段全PASS后才执行一次O单股原生验收。**

## 七、下一动作

Run015增量NEXT_ACTIONS已写入`docs/runtime/RUN015_NEXT_ACTIONS.json`。为避免用不完整响应覆盖已有17926字节的`docs/DSA_NEXT_ACTIONS.json`，本轮没有重写该大文件；Run015恢复时应先读原`DSA_NEXT_ACTIONS.json`，再应用`RUN015_NEXT_ACTIONS.json`的最新Owner override。

最高原则继续是：**Accepted Progress > Activity**。当前正确结果是O WAIT、U WAIT、无Top3、无模拟成交，而不是绕过Drive Gate制造一个“有输出”的原生调用。
