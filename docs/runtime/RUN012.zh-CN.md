# Run012｜O 原生私密执行桥接验收与待执行门槛

Run ID：`TRI-DSA-EXEC-20260914-012`

状态：**工程准备通过；O 原生模型验收未通过；U 正式逐股验收未启动；模拟入场不具备资格。**

## 1. 恢复基线

- O 冻结上游：`089d9d26d68f8b839ea5a74a3784e4402925f8b7`，不得修改原生代码、提示词和评分。
- Run008 `34816572611` 仅为 A0/A3 研究冻结，不替代 O 原生验收，不覆盖既有预测。
- Run009 为本地准备与来源诊断，无正式 O 原生调用。
- 权威模拟账仍为 `DSA_DUAL_SIM_BC_V1_20260914`；Journal / Summary 当前均为 Library version 1，U/O 各 300000 CNY、零持仓、零成交，已有四个信号事件均为 WAIT。本轮没有合格 BUY，不改账本。

## 2. 已完成：既有私密目的地核验

发现并复用已有私密仓库 `Rupheal/TRIDENT-Foundation`，未新建主机、未新购服务。

新建研究分支：`research/dsa-private-evidence-20260914`。

完成无模型、无密钥合成保存探针：

- 文件：`evidence/dsa/private_persistence/TRI-DSA-EXEC-20260914-011.synthetic.json`
- 写入后重新读取；SHA-256 一致：`2682c451bd4bc164073bf6a307bf5018d0134708911060aeffcafcd3dc4af0a5`
- 该探针只证明现有 ChatGPT/GitHub 连接可对该私密研究分支执行保存、读取和哈希回验；不等于公开 DSA Actions runner 已取得跨仓库写权限。

TRIDENT-Foundation 的现有结构规则要求大型原始数据、付费数据转储、密钥、归档/二进制备份不进入 Git history。本轮仅计划保存 O 单股验收所需的有界文本证据；不保存密钥，不把该仓库当作大型数据湖。

## 3. 已完成：公开 runner → 私密仓库桥接工程

在 `fix/native-private-execution-20260914` 增加：

- `scripts/dsa_private_github_store.py`
  - 只从环境变量读取 `DSA_EVIDENCE_TOKEN`，不接受命令行 token；
  - 先确认目标仓库为 private；
  - 仅允许 append-only 新路径；拒绝覆盖；
  - 写入后重新读取并逐文件 SHA-256 回验；
  - stdout 只输出路径、字节数、哈希和 commit/blob 元数据，不输出 payload。
- `tests/test_dsa_private_github_store.py`
  - 私密性门槛、路径穿越、append-only、写入/读取/哈希一致性测试。

云端准备运行 `34839837799`：18/18 deterministic tests PASS；`DEEPSEEK_API_KEY` 存在；`DSA_EVIDENCE_TOKEN` 当前不存在；模型请求 0；余额未读取；O 原生验收仍为 false。

## 4. 已完成：O 单股原生私密执行工作流已就绪并保持 disarmed

新增 `.github/workflows/43-native-private-single-stock.yml` 与 `docs/runtime/run012-private-native-arm.json`。

工作流顺序固定为：

1. arm + 两个 credential presence gate；
2. runner 对私密仓库执行无敏感内容的 save/read/hash 探针；
3. 读取 DeepSeek 当前 CNY balance，实际余额仅写入私密证据，公开日志只披露可用/是否覆盖预算；
4. 在模型调用前把 balance preflight 私密持久化；
5. checkout 冻结上游 `089d9d26...`，运行既有预算/原生数据回归和小米独立数据 gate；
6. 调用既有 `run_hk_original_model_probe.py`，其底层仍执行冻结上游原生 `main.py` / `GeminiAnalyzer`；
7. 原始 stdout/stderr、原始 input/result、provider response、request body、budget、preflight 全部先落 runner 临时目录，**不上传公开 artifact、不向公开日志输出原始模型内容**；
8. 整个有界证据目录写入 TRIDENT 私密研究分支并逐文件读回哈希验证；
9. 公开日志仅发布冻结 commit、请求数、输入 gate、usage peak estimate、进程状态等脱敏元数据；
10. 完成后还必须由高能力模型读取私密 result/input/provider 证据进行人工语义复核，才能宣布 O 单股 PASS。

Disarmed 云端运行 `34839998620` 已验证 fail-closed：在 arm gate 即停止；模型、余额、私密写入和后续步骤全部 skipped。模型请求 0。

## 5. 当前唯一外部阻塞项

公开 `Rupheal/daily_stock_analysis` runner 的现有 `GITHUB_TOKEN` 不能写另一个私密仓库；ChatGPT 的 GitHub 连接凭据也不能注入 GitHub Actions。当前缺少的是一个**只允许写现有 `Rupheal/TRIDENT-Foundation` 的跨仓库凭据**。

最小配置名称已经固定为：`DSA_EVIDENCE_TOKEN`。

要求：fine-grained GitHub token，仅选择 `Rupheal/TRIDENT-Foundation`，Repository permissions 只给 `Contents: Read and write`；不要授予 `daily_stock_analysis`、账户级广泛权限、Actions 管理权限或其他仓库权限。token 只放入 `Rupheal/daily_stock_analysis` → Actions repository secret `DSA_EVIDENCE_TOKEN`，不得粘贴到聊天或提交到 Git。

配置完成前，`run012-private-native-arm.json` 必须保持 `armed:false`。

## 6. 后续自动顺序

Owner 完成上述单个 secret 配置后：

- 更新 arm 为 true 并触发 Run012；
- 先验私密 runner 路径、当前余额，再执行一次必要的 O 单股原生调用；
- 私密证据回读 + 哈希 + 人工语义复核通过后才计算实测单股资源；
- 随后按当期官方沪深港股通可买股票并集重新冻结 O 分母并制定全池预算/运行；保留逐股失败与原生观察/卖出立场；全池完整且分数可比后才排序；
- O 全池完成后才进入 U45 正式逐股验收；
- 只有出现合格 BUY 且费用/汇率/整手/发布后可交易价格齐备，才允许更新模拟账；否则继续 WAIT。

## 7. 资源与通过项

本 Run012 截至本文件：

- 新 DeepSeek 模型 HTTP requests：0
- 新 DeepSeek 模型费用：0 CNY
- 工程通过：私密目的地合成 save/read/hash PASS；桥接离线+云端 tests 18/18 PASS；disarmed fail-closed PASS
- 模型报告通过：NO（尚未执行新的私密 O 原生单股）
- 策略有效性通过：NO / N/A（未来窗口未成熟）
- U 正式执行验收：0/45
- 模拟入场：NOT_ELIGIBLE_NO_QUALIFIED_SIGNAL

main 未合并；定时任务未修改；Run005/Run008 冻结结果未改；未创建真实订单。
