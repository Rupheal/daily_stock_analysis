# Run012｜O 原生私密执行桥接验收与待执行门槛

Run ID：`TRI-DSA-EXEC-20260914-012`

状态：**工程准备通过；O 原生模型验收未通过；U 正式逐股验收未启动；模拟入场不具备资格。**

## 1. 恢复基线

- O 冻结上游：`089d9d26d68f8b839ea5a74a3784e4402925f8b7`，原生代码、提示词和评分不得修改。
- Run008 `34816572611` 是 A0/A3 研究冻结，不替代 O 原生验收，不覆盖既有预测。
- Run009 是本地准备/来源诊断，无新的正式 O 原生验收调用。
- 权威模拟账为 `DSA_DUAL_SIM_BC_V1_20260914`；Library 中 Journal / Summary 均为 version 1，U/O 各 300000 CNY、零持仓、零成交、各 2 次 WAIT。本轮没有合格 BUY，因此不修改账本。

## 2. 私密保存目的地已核验

复用已有私密仓库 `Rupheal/TRIDENT-Foundation`，未新建主机、未购买新服务。新建研究分支 `research/dsa-private-evidence-20260914`。

合成探针（无模型内容、无密钥）：

- `evidence/dsa/private_persistence/TRI-DSA-EXEC-20260914-011.synthetic.json`
- 写入、读取、SHA-256 回验一致：`2682c451bd4bc164073bf6a307bf5018d0134708911060aeffcafcd3dc4af0a5`

该探针证明现有 ChatGPT/GitHub 连接可以对私密研究分支保存/读取/哈希回验；它不代表公开 DSA Actions runner 已获得跨仓库写权限。

TRIDENT 规则要求大型原始数据、付费数据转储、密钥、归档/二进制备份不得进入 Git history。因此 O 单股验收只允许保存有界文本证据。

## 3. runner → 私密仓库桥接工程已通过

在 `fix/native-private-execution-20260914` 增加：

- `scripts/dsa_private_github_store.py`：仅从 `DSA_EVIDENCE_TOKEN` 环境变量取凭据；验证目标仓库为 private；append-only；拒绝覆盖/路径穿越；写后读回逐文件 SHA-256；不打印 payload。
- `scripts/dsa_prepare_private_text_export.py`：只导出批准的文本证据；显式排除 `original-data.db` 等 SQLite/二进制文件；生成文本证据 manifest 和哈希。
- 对应单元测试覆盖 private gate、append-only、hash round-trip、路径穿越、二进制排除/拒绝。

云端准备运行 `34840354012`：**20/20 deterministic tests PASS**；`DEEPSEEK_API_KEY` 存在；`DSA_EVIDENCE_TOKEN` 当前不存在；模型请求 0；余额未读取；O 原生验收仍为 false。

## 4. O 单股原生私密工作流已就绪并保持 disarmed

`.github/workflows/43-native-private-single-stock.yml` 已注册。固定顺序：

1. arm + 模型/私密证据 credential gate；
2. runner 先做无敏感内容的私密 save/read/hash；
3. 读取当前 DeepSeek CNY balance；真实余额只进入私密证据，公开日志仅给是否满足预算；
4. 模型调用前先私密保存 balance preflight；
5. checkout 冻结上游 `089d9d26...` 并运行既有预算、原生数据回归、小米独立数据 gate；
6. 调用既有 `run_hk_original_model_probe.py`，底层仍执行冻结上游原生 `main.py` / `GeminiAnalyzer`；
7. 原始 stdout/stderr、input/result、provider response、request body、budget、preflight 均只落 runner 临时目录，不上传公开 artifact，不在公开日志输出原始模型内容；
8. 通过 text-only exporter 只复制批准的文本证据，SQLite 数据库不进入私密 Git history；
9. 有界文本证据保存至 TRIDENT 私密研究分支并逐文件读回哈希验证；
10. 公开日志只发布冻结 commit、请求数、输入 gate、usage peak estimate、进程状态等脱敏元数据；
11. 最后仍需高能力模型读取私密 input/result/provider 证据做人工语义复核，才可宣布 O 单股 PASS。

最新 disarmed 云端运行 `34840389904` 在第一道 arm gate 即停止；余额、模型、私密原始证据步骤全部 skipped；模型请求 0。该 fail-closed 行为通过。

## 5. 当前唯一需要 Owner 操作的阻塞项

公开 `Rupheal/daily_stock_analysis` runner 的默认 `GITHUB_TOKEN` 不能写另一个私密仓库；ChatGPT 的 GitHub 连接凭据也不能注入 GitHub Actions。当前只缺一个限定范围的跨仓库凭据，secret 名称固定为：`DSA_EVIDENCE_TOKEN`。

要求：fine-grained GitHub token，仅选择 `Rupheal/TRIDENT-Foundation`，Repository permissions 只给 `Contents: Read and write`。不要授予 `daily_stock_analysis`、Actions 管理权限、账户级广泛权限或其他仓库权限。token 仅保存到 `Rupheal/daily_stock_analysis` 的 Actions repository secret `DSA_EVIDENCE_TOKEN`；不得粘贴到聊天或提交到 Git。

配置完成前，`docs/runtime/run012-private-native-arm.json` 必须保持 `armed:false`。

## 6. 配置后的自动接续顺序

Owner 完成上述单个 secret 后：

- arm Run012；先验证 runner→私密仓库和当前余额，再做一次必要的 O 单股原生调用；
- 私密保存/读回/哈希 + 人工语义复核全部通过后，依据实测单股资源制定 O 全池预算；
- 重新冻结当期官方沪深港股通可买股票并集，执行 O 全池，保留完整分母、逐股失败和原生观察/卖出立场；只有全池完成且分数可比才机械排序；
- O 全池通过后才启动 U45 正式逐股验收；分母仍为45，失败隔离，0—3只候选，不编分不凑数；
- 只有合格 BUY、费用/汇率/整手及信号发布后的实际可交易价格全部齐备，才允许更新权威模拟账；否则继续 WAIT。

## 7. 本轮资源与通过项

- 新 DeepSeek 模型 HTTP requests：**0**
- 新 DeepSeek 模型费用：**0 CNY**
- 工程通过：私密目的地合成 save/read/hash PASS；text-only bridge 云端 **20/20 PASS**；disarmed fail-closed PASS
- 模型报告通过：**NO**（尚未执行新的私密 O 原生单股）
- 策略有效性：**N/A / 未成熟**
- O 正式原生验收：**0（待单股）**
- U 正式执行验收：**0/45**
- 模拟入场：**NOT_ELIGIBLE_NO_QUALIFIED_SIGNAL**

main 未合并；定时任务未修改；Run005/Run008 冻结结果未改；未创建真实订单。
