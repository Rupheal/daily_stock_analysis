# DSA 双模型接续执行验收｜Run033—040

日期：2026年9月17日。行情目标日：9月16日已完成交易日；官方股池生效日：9月17日。以下是本轮真实执行结果，不是盘前预测，也不追填今天上午的信号或成交。

**结论：数据恢复、原版数据实跑、私密保存和错误输出拦截取得通过项；O原生全池出票、U正式逐股信号及真实信号驱动的模拟成交仍未完成。运行授权已批准、已激活，不是本轮阻塞原因。**

## Accepted Progress

| 环节 | 本轮真实结果 | 验收边界 |
|---|---|---|
| 当期O股池 | 官方可买股票并集660；完整分母保留 | 不含被排除ETF，不借用U45 |
| 全历史输入保存 | Run033保存全池可复用行情证据；严格整段独立核对356/660 | 与21日特征恢复采用不同口径，不互相替代 |
| Run006规则用于新时点 | 598/660只达到21日输入要求，445保留原源、153按既有规则恢复 | 不等于预测正确率；旧638/660属于较早时点 |
| 原版实际消费恢复输入 | 冻结上游真实CLI执行，598成功、62隔离 | 无LLM的原版数据实跑，不冒充原生全池评分 |
| U逐项审阅 | 45/45已审阅；44/45当前可买、行情事实可复用；30/45通过完整历史窗口严格核对 | 09618保留分母但不进入当前可买子集；正式信号仍0/45 |
| U可复用事实 | 44只涨跌幅、MA5/10/20、日量比已按原始序列复算 | 有来源和数据日期；没有伪造行情秒级时间、分数或买点 |
| 私有原件保存 | 分包保存、逐包读回、完整重组及哈希通过；快手37个原始文件已独立复原 | 原始Prompt/Response不进入公开仓库；腾讯失败原件缺口如实保留 |
| 错误输出拦截 | 104项云端测试通过、0失败、0跳过；真实失败原件离线回放通过 | 拒绝空正式回答、截断回答及原生失败兜底，不改变原版策略 |
| 权威账本 | 固定提交读取账本与摘要，完整重放一致 | 两账户各现金30万元、持仓0、买卖0；本轮不写入虚假成交 |

这些分母不能平均成一个“项目完成率”。当前数据恢复为598/660（90.6%），U行情事实为44/45（97.8%）；本时点正式O输出接受数仍0/660，U正式信号接受数仍0/45，整个交易闭环尚未验收。

## 两次真实原生请求及修复

1. **腾讯00700：**价格预检及全部43条原生日线核对通过，发生1次真实请求。返回结果被开盘方向矛盾等门槛拒绝，原始证据保存报`StoreError`。现存Claim、脱敏回执与费用记录已封存，不重新生成“原件”。旧日志未细分保存异常，不能断言唯一原因；后续同规模真实原件回放明确复现单包容量上限问题，并完成分包修复。
2. **快手01024：**这是此前未调用的剩余样本。先通过免费预演和私密保存门槛，再发生1次真实请求。四个分包及索引保存/读回/哈希成功，并独立复原4,908,132字节、37个文件。
3. **快手的实际失败原因：**私有SSE原件证明输出以`finish_reason=length`结束，8192个输出token均用于推理，正式回答字符数为0；原版返回`success=false`及错误兜底。不能把该默认意见或默认分数当作成功分析。
4. **免费修复：**新增供应商回答完整性检查；`O_POST_OUTPUT_CONTRACT_v4`必须确认原生成功且无错误；发布适配器禁止包装原生错误结果。另补齐未来通用预检应携带的既有执行契约版本号。旧输入和旧输出不回写，原版上游代码、提示词和评分不改动。

Run040只用既有失败原件进行回放，没有第三次付费调用。原版模型配置与输出预算的兼容性仍需研究；没有将任何不同配置自动晋级为正式策略。

## 剩余缺口

- **O输入62只：**57只原始价格冲突、3只缺目标日日线、1只成交量冲突、1只不足21日。57只中39只只在历史开盘价上冲突。现有港交所免费日报不提供开盘价，不能拿它给开盘价冲突签收。逐股名单及失败原因保存在[隔离清单](RUN035_CONT1_ISOLATIONS.json)和[冲突定位](RUN035_CONT1_RAW_CONFLICT_DIAGNOSTIC.json)。
- **O正式出票：**还需通过逐股新闻/风险证据、完整原生回答及语义验收，再汇总全池机械排序。少数技术样本和598只数据准备不能称全池Top3。
- **U正式出票：**新闻检索44只已有回执，但本次仅2只具有已复核的有限发行人原始公告；完整风险覆盖、宏观仓位约束、U正式信号及交易计划的验收产物仍缺。现有技术研究评分组件不能冒充完整U出票器。详见[45股逐项验收](RUN038_U45_HANDOFF_AUDIT.json)与[可复用行情事实](RUN038_U45_PUBLIC_FACTS.json)。
- **真实信号交接：**目前没有通过自身门槛且及时冻结的BUY信号，不能人为制造首次模拟成交。未来还须有决策后的真实报价时间、可成交卖一价/数量、整手、费用与汇率证据。

## 真实运行与产物

| Run | GitHub运行 | 结果 |
|---|---|---|
| Run033 | [35180179743](https://github.com/Rupheal/daily_stock_analysis/actions/runs/35180179743) | 全池历史缓存与严格核对 |
| Run035-CONT1 | [35181701358](https://github.com/Rupheal/daily_stock_analysis/actions/runs/35181701358) | 当期598/660输入恢复 |
| Run036 | [35182213024](https://github.com/Rupheal/daily_stock_analysis/actions/runs/35182213024) | 原版CLI实际消费598只输入 |
| Run034-CONT2 | [35181806937](https://github.com/Rupheal/daily_stock_analysis/actions/runs/35181806937) | 腾讯请求失败并隔离；工作流成功不代表模型验收成功 |
| Run037-CONT1 | [35182844843](https://github.com/Rupheal/daily_stock_analysis/actions/runs/35182844843) | 免费私密分包回放及两股预演通过 |
| Run039 | [35183191883](https://github.com/Rupheal/daily_stock_analysis/actions/runs/35183191883) | 快手请求失败并隔离，原件保存/恢复通过 |
| Run040 | [35183873567](https://github.com/Rupheal/daily_stock_analysis/actions/runs/35183873567) | 104项免费回归测试通过 |

首次失败的来源参数、过宽预检窗口、缺失成交额NaN以及Run037工作流绑定错误均保留记录。修复后接续原缓存，未重做已经验收的Run005预测或既有模拟规则。

## 资源与费用

- 新增DeepSeek模型HTTP：**2次**；两次均未通过正式输出验收。
- 保守高峰用量估算：腾讯0.068136元、快手0.076852元，**合计0.144988元人民币**。
- **实际可归因扣费未核实。** 余额接口本次未显示变化，不能据此称零费用；平台基础资源费用不可见。
- 免费修复、回放和数据采集未新增付费数据或订阅；未自动充值。详见[本轮费用台账](RUN033_040_COST_LEDGER.json)。

本数额是两次验收失败请求的估算费用，不是一次成功O全池扫描报价。旧的低成本定制技术评分开销，也不能用作原版长报告全池费用。

## 接续安排与权限

统一入口为[DSA_NEXT_ACTIONS.json](../DSA_NEXT_ACTIONS.json)的`current_execution`。更早记录中的“未激活”、数据660齐备或638可用均需按其原时点和原范围解读，不能覆盖当前结果。

既有香港时间09:00、16:30及复盘时点保持。本轮交给既有任务的是最新状态入口、可追溯的分层数据和失败清单；**没有把每日O全池调用、U正式出票或首次自然成交标成已自动跑通。** 9月17日16:30的简报需要重新核验9月17日收市数据，本轮9月16日缓存不能冒充当天收盘。今天上午已过期，不补发可成交信号。

后续研究优先处理：原版配置/完整回答兼容性；有开盘价字段的第三来源核对；U独立信号、新闻风险、宏观与计划生产器；最后以真正合格的新信号验证Entry-v1。保持Run005 A0/A3与未成熟窗口不动。

本轮没有新的必要权限申请；已批准的运行窗口无需再次批准。正式策略晋级仍须形成版本对照后由Owner确认。main、Production、真实订单和其他项目任务均未改动。
