# RUN018｜O 原生会话边界根因与零 HTTP 恢复资格

## 状态

- 分支：`fix/native-private-execution-20260914`
- 冻结上游：`089d9d26d68f8b839ea5a74a3784e4402925f8b7`
- O arm：`false`
- Run005 / Run008：未修改
- DeepSeek HTTP requests（本阶段）：0
- DeepSeek tokens（本阶段）：0
- DeepSeek 模型增量费用（本阶段）：CNY 0
- U 正式验收：0/45
- 模拟交易：NOT_ELIGIBLE / WAIT
- 固定 legacy 验收：12/16；策略有效性仍未验收

## 已确认的上游 O 失败事实

GitHub Actions Run `34985098001` 已在 Drive、余额、冻结回归、Xiaomi preflight、durable claim 等门槛之后进入冻结 O，但在模型 HTTP 之前失败。私密 canonical bundle 已保存并完成独立读回、SHA-256 与 restore。

Read-only reconciliation Run `34985662112` 确认：

- canonical CLAIM = 1
- canonical native artifact = 1
- archive SHA verified = true
- bundle manifest restore verified = true
- native process exit code = 1
- input_validated = false
- model_http_requests = 0
- provider request body absent
- provider response absent
- original input absent
- original result absent
- fixed error code = `NATIVE_SESSION_MISMATCH`
- repeat_model_request_permitted = false

因此不能把 CLAIM 删除、覆盖或视为不存在，也不能自动重复模型请求。

## NATIVE_SESSION_MISMATCH 最终根因

Model-free frozen-native session diagnostic 确认，冻结原生 CLI 在目标完整交易日 `2026-09-15` 的验收上下文中，实际存入数据库的 HK01810 最新日线为 `2026-09-14`，状态 `invalid_or_stale`；provider attribution 明确为 `YfinanceFetcher`。诊断没有模型凭据，模型请求为 0。

随后核对冻结 `data_provider/yfinance_fetcher.py`：其历史调用为：

```python
yf.download(
    tickers=yf_code,
    start=start_date,
    end=end_date,
    progress=False,
    auto_adjust=True,
    multi_level_index=True,
)
```

Run `34987234496` 使用同一 yfinance 1.7.0 调用形态做相邻 end A/B，结果：

- `end=2026-09-15` → latest=`2026-09-14`
- `end=2026-09-16` → latest=`2026-09-15`
- `boundary_pattern_confirmed=true`
- model requests = 0

结论：失败不是 9/14 被时区误标成 9/15，也不是 observer 应放宽 session；而是 Yahoo/yfinance 的 `end` 为排他边界，冻结原生路径在当日把目标交易日作为 end 时排除了该目标日 K 线。严格 session gate 必须保留。

## Volume gate 当前正式状态

此前 Xiaomi 单例 125 个重叠 session 中 124 个 volume exact match，仅最新 session 存在小幅 provider 差异；随后已完成港股 cross-provider calibration 并冻结统一 reconciliation contract。不能再把 Run017 的 O 失败归因于 volume。

O observer 已重构为共享确定性 `o_native_input_contract.py`，要求：

- native session 必须与已验收 preflight target 完全一致；
- OHLC 保留严格门槛；
- volume 仅在携带已验收 calibration id / bound / unit semantics 时允许 latest-session bounded reconciliation；
- 100x / 0.01x、非有限值、超界、错误 session 均 FAIL。

云端 model-free observer/recovery tests：30/30 PASS；O 保持 disarmed，无模型 key，无模型请求。

## CLAIM 恢复政策

新增 fail-closed `o_zero_http_recovery_policy.py`。它不会自行授权请求。

Read-only eligibility Run `34987354728` 重新读取 canonical Drive evidence 并得到：

- `evidence_recoverable=true`
- `state=RECOVERABLE_BUT_NOT_AUTHORIZED`
- `reason_count=0`
- `owner_authorized=false`
- `request_authorized=false`
- `maximum_successor_http_requests=0`
- `fixed_error_code=NATIVE_SESSION_MISMATCH`
- `model_http_requests=0`
- canonical CLAIM/native 必须保留
- canonical IDs 不得删除或覆盖
- 如未来授权 successor，必须使用新的 append-only child evidence artifact

只有显式 recovery authorization 后，policy 才能允许最多 1 次 successor HTTP request；授权前上限仍为 0。

## 下一步硬门槛

即使 Owner 未来授权一次 successor attempt，也不能立即请求模型。successor workflow 必须先在同一运行窗口完成：

1. O 仍处于受控 recovery 状态；
2. 完整 fresh Xiaomi independent preflight PASS；
3. 冻结 native data-only smoke 的 latest session 必须等于 preflight target；
4. observer contract tests PASS；
5. fresh unique Drive save/read/SHA/restore PASS；
6. canonical CLAIM/native 仍各为 1 且完整，不删除、不覆盖；
7. current DeepSeek balance readable and affordable；
8. frozen upstream commit 仍精确等于 `089d9d26d68f8b839ea5a74a3784e4402925f8b7`；
9. 只有以上全部 PASS 才允许一个 successor HTTP request；
10. successor raw evidence 必须保存为新的 append-only child artifact，并在人工语义验收前完成 readback/SHA/restore。

如果同一时点 frozen native data-only smoke 仍落后 preflight target，则保持 WAIT，不调用模型。

## 当前 Owner 最小决策

当前不存在 OAuth、Drive Secret、充值或数据购买操作。唯一尚未授权的动作是：是否允许在上述全部硬门槛再次即时通过后，执行 **最多 1 次** O-native successor DeepSeek HTTP request，用于完成此前 0-HTTP claim 的受控恢复验收。
