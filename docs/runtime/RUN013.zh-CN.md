# Run013｜Drive保存工程与U缓存准备

Run ID：TRI-DSA-EXEC-20260914-013。Parent：c71e019e66f54854b7e663a3f987ffda86e9451a。
范围：保存适配器/工作流/限定OAuth配置说明，U45缓存逐股核验。预算：新模型请求0，新付费服务0；准备预估20—35分钟，Owner授权等待不计入。平台额度/费用不可直接读取。

## 结果
- Drive目录权限复核：仅owner、未共享。连接器写入U45_PREPARATION新JSON成功并元数据读回；这不证明runner授权或字节哈希回验。
- 22项本地针对性测试通过（Drive18＋既有U4）；编译与YAML结构通过。云端验证另记回执。
- 43号原生工作流改用Drive原始字节保存，arm=false。44号零模型合成准备仅在四项Drive配置齐备时执行真实上传恢复。原有GitHub私密有界文本桥保留，不自动切换。
- 归档保留原始字节并生成逐文件哈希清单；追加式Artifact ID、同内容幂等、冲突拒写、权限门槛、独立读回及临时目录恢复；所有公开日志仅含状态。容量限制4MiB；超过明确失败，不截断，不自动扩展。
- 来源时间、取得时间、父artifact字段保留；缺失用null＋原因，不用文件mtime或retrieved_at替代price_time。真实证据必须在人工语义验收时补齐来源链。
- 此实现是单写者、应用层只追加及篡改检测，不宣称Drive WORM或跨任意外部写者事务保证。

## U45真实增量
复用Run009原件并核验来源收据哈希；不重新抓全池。
深交所XLSX错误尺寸元数据会使read-only读取只返回表头；reset_dimensions后取得691条，与同日JSON元数据recordcount一致。

- 已逐股审阅45/45；45份Yahoo原始收据哈希匹配。
- 45/45匹配港交所当日证券表身份、ISIN、整手、币种。
- 44/45有2026-09-14 SSE股票买入标记；09618京东在当日SSE/SZSE快照中均缺席，保留U成员但隔离买入资格。
- 完整准备0/45；缺官方停牌证据、复权公司行动、成交量单位独立证明、当期新闻公告与资金、交易计划/费用。
- 这些是2026-09-14来源的准备证据，不是下一交易日现时信号；不回填历史快照，不生成分数/Top3或BUY。

## 待Owner最小动作
详见 `DRIVE_OWNER_SETUP.zh-CN.md`。采用自用Desktop OAuth＋Google Picker，scope仅drive.file，明确选择既定文件夹，四个值只进入Actions Secrets。浏览器授权流程尚未实测，禁止扩大整盘权限来绕过失败。

## 通过口径与限制
程序调整已完成；runner保存/真实恢复未验收；O原生单股未验收；U完整准备0/45；策略有效性未成熟。旧固定清单12/16=75%为限定历史事项，未因新增工程工作提升。

没有修改main、定时任务、Run005/Run008预测或权威Journal/Summary。ESSD R本批复制/恢复没有回执，NAS仍PENDING_SETUP。
回滚仅恢复本次代码/工作流变更，保留已保存原件、版本和历史记录，不重置账户。

## 云端实测回执

提交7db7d23ee0f9301be67de2eee06a9043c9cda2a2。44号运行34862431389在2026-09-14 15:29:22 UTC结束：18项Drive测试通过；DSA_DRIVE_FOLDER_ID、DSA_DRIVE_CLIENT_ID、DSA_DRIVE_CLIENT_SECRET、DSA_DRIVE_REFRESH_TOKEN均未配置，真实上传/读回/恢复步骤skipped。43号运行34862431286在arm=false处停止，所有模型步骤skipped。新增模型请求0。Owner操作已缩小为限定文件夹OAuth＋四项Actions Secrets；不再重复缺配置检查。

U45准备JSON已由连接器保存到既定私密Drive目录，元数据读回确认application/json、98470字节、未共享。原件身份/哈希与逐股源收据保存在该私密文件中；不是runner恢复回执。

U媒体补查：复用既有公司匹配解析器，一次取得3份免费地区RSS，3/3 HTTP200且解析成功；按45股逐一匹配，3股取得媒体线索。它们只是待原始公告核实的线索，不使新闻/正式准备通过数增加。来源原字节、取得时间及SHA-256另存私密媒体证据包；未回填既有预测。
