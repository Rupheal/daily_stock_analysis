# Run016｜Drive四项Secrets已可见；新写入读回稳定性修复；O原生继续封锁

Run ID：TRI-DSA-EXEC-20260915-016

## 权威状态

- 分支：`fix/native-private-execution-20260914`
- 本轮恢复自Run015；Run008 A0/A3冻结结果未修改。
- 四项Drive配置在GitHub Actions runner中均已可见：`DSA_DRIVE_FOLDER_ID`、`DSA_DRIVE_CLIENT_ID`、`DSA_DRIVE_CLIENT_SECRET`、`DSA_DRIVE_REFRESH_TOKEN`。公开日志只记录present/missing，不记录任何值。
- private GitHub fallback旧arm保持解除；不得绕过Drive主路径。
- O原生模型请求本轮截至本回执仍为0；DeepSeek tokens/cost仍为0。

## 真实44号证据

GitHub Actions run `34978665852` / job `104412930449`：

1. 40项Drive/U恢复/工作流门禁测试通过。
2. Drive四项配置presence gate通过，missing list为空。
3. 历史Drive原件 `ART-DSA-RUN013-PROBE` 与 `ART-DSA-RUN013-RESTORE` 均完成：单一命中、HTTP 200、下载字节与记录size一致、SHA-256匹配、read前后version稳定。
4. fresh unique storage artifact `ART-DSA-STORAGE-34978665852-1` 创建后，在即时恢复步骤失败，旧实现只返回 `READBACK_HASH_OR_VERSION_MISMATCH`，因此无法把失败归因为内容hash还是provider version瞬时变化。
5. 因fresh unique save/read/hash/restore未全过，Drive acceptance仍不得记PASS；43号O原生调用不得执行。

另有早先43号run `34977724113` 同样在模型请求前的runner-side Drive probe失败，所有模型步骤均skipped；这证明本轮没有产生重复DeepSeek调用。

## 本轮工程修复

为不降低完整性门槛，只修复fresh-create后的provider metadata稳定时序：

- 新增 `stable_private_meta()`；新写入文件必须出现两次连续相同且为数字的Drive version后，才允许读取media。
- 读取后仍必须满足：内容SHA-256完全匹配；post-read version与稳定pre-read version完全一致。
- 若version持续变化，明确失败为 `READBACK_VERSION_NOT_STABLE`；若内容不匹配，明确失败为 `READBACK_HASH_MISMATCH`；若读后version变化，失败为 `READBACK_VERSION_CHANGED`。
- 新增回归测试覆盖version从1→2后稳定于2的provider传播情景；没有取消CAS/version检查，也没有把hash失败降级为warning。

修复提交：
- `1b9d16ced6decd19ed57be915e2e820550df38b4` — wait for Drive version stability before readback
- `21a8a0dc69538e853ddbd29267b4db26d672c99c` — regression test for transient Drive version settling

## 当前验收状态

- Drive Secrets presence：4/4 PRESENT
- 历史Drive read/hash/version：PASS（两项历史artifact）
- fresh real save：已发生，但完整fresh roundtrip未验收
- independent read：fresh artifact未验收
- SHA-256 match：fresh artifact未验收
- disk restore：fresh artifact未验收
- Drive整体：`NOT_ACCEPTED_PENDING_FRESH_ROUNDTRIP_AFTER_FIX`
- O native：0，`BLOCKED_BY_DRIVE_ACCEPTANCE`
- O Top3：不可正式使用
- U正式验收：0/45；Run015冻结的U45恢复边界继续有效
- fixed legacy acceptance：仍12/16=75%
- 模拟交易：NOT_ELIGIBLE

## 下一最小自然触发

只接受由本次修复提交触发的44号storage-only工作流结果。必须先得到fresh artifact的：REAL_SAVE PASS + INDEPENDENT_READ PASS + SHA256_MATCH PASS + DISK_RESTORE PASS；随后才进入O调用前CLAIM/call-intent/旧产物对账。Drive未4/4前模型请求必须继续为0。

本回执不修改main、不修改定时任务、不创建真实订单、不重写Run005/Run008、不启用fallback、不把workflow green等同于Drive PASS。
