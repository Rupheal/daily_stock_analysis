# Run017｜Drive 4/4正式通过；O首次受控验收在模型前输入契约处封锁

Run ID：TRI-DSA-EXEC-20260915-017。

## 结论

- Drive Fresh-Write真实终验已经PASS：SAVE / READBACK / SHA-256 / RESTORE = 4/4。
- 小米模型-free数据门禁已经PASS。
- 一次性O调用流程已创建并持久化CLAIM；随后进入冻结原生probe。
- 本次实际DeepSeek HTTP请求数为0，不是1。模型没有被调用，tokens=0，增量模型费用=0 CNY。
- 原生probe在HTTP请求前失败：`native_process_exit_code=1`、`input_validated=false`。
- 失败现场、preflight、过程文件、CLAIM相关回执等原始字节已经私密Drive保存，并完成save/read/hash/restore。
- workflow最后的sanitized acceptance步骤因此失败；不能把GitHub步骤名称“One frozen-upstream native request”显示success误解为模型请求成功，该步骤显式捕获底层退出码以保证先保存失败证据。
- arm已在提交 `992b7e895d9aff63fe031343ad530bbe4e443d42` 重新设为false。不得自动重试。

## Drive与preflight证据

Drive 44号真实终验：run 34979881950 / job 104417113686；fresh bundle完整roundtrip通过。后续44号run 34984976974继续通过更严格的Drive回归/static检查与唯一fresh roundtrip。

模型-free小米preflight：run 34984357499 / job 104432522239，PREFLIGHT_PASS，模型请求0。跨供应商成交量诊断显示125个重叠交易日中124日精确一致；最新日腾讯164,256,645股、Yahoo 164,214,900股，差41,745股，约0.0254%，落在已校准的最新交易日0.2%供应商修订容忍上限内，不是100倍/0.01倍单位错位。

## O受控运行

43号run 34985098001 / job 104435051002：

1. arm/credential gate PASS；
2. runner-side Drive save/read/hash probe PASS；
3. native artifact/CLAIM重复门禁在调用前PASS；
4. DeepSeek余额可读，单股2 CNY上限可负担；余额原件已私密保存；
5. frozen original data/observer regression PASS；
6. fresh Xiaomi data gate PASS；
7. durable one-call CLAIM已先行保存并验证；
8. 原生probe开始后在模型HTTP请求前失败；
9. 私密原始证据bundle保存/读回/hash/restore PASS；
10. sanitized acceptance发布按fail-closed规则失败。

完整日志最终公开汇总明确：`model_http_requests=0`、`input_validated=false`、`native_process_exit_code=1`、`request_status=null`。

## 根因

根因不是Drive，也不是DeepSeek API，而是两个输入验证层规则没有同步：

- 新的模型-free preflight使用已经校准的港股成交量reconciliation：历史交易日要求精确一致；仅最新交易日允许供应商修订差异≤0.2%，单位仍固定shares且无换算。
- `scripts/run_hk_original_model_probe.py` 的observer仍保留旧判断：`abs(native_volume - expected_volume) <= 0.5`股。
- 小米最新交易日两源差异41,745股虽只有约0.0254%，可通过校准preflight，却必然超过旧observer的0.5股绝对门槛。

因此observer在`validated=True`之前拒绝输入，ResearchBudget不会放行HTTP请求。这正是为什么CLAIM已经存在，但DeepSeek HTTP请求仍为0。

## 正式状态

- Drive storage：PASS 4/4
- O native：BLOCKED_RECONCILIATION
- DeepSeek requests：0
- DeepSeek tokens：0
- incremental DeepSeek model cost：0 CNY
- durable CLAIM：存在
- canonical private evidence artifact：存在并恢复验证通过
- arm：false
- O single acceptance：未PASS
- O current Universe：本轮不重新冻结（因O single未PASS）
- O Top3：不可正式使用
- U formal：0/45；Run015的U45恢复边界不变
- simulation：NOT_ELIGIBLE
- fixed legacy：仍12/16=75%；不能把本次pre-model失败计成G4.3通过

## 下一最小动作

下一轮只修observer输入验证契约，使其消费已经冻结、经证据校准的成交量reconciliation结果，同时保持：双源验证、shares单位、历史日零容忍、最新日仅≤0.2%供应商修订、异常比例hard fail。先做model-free回归和只读CLAIM/native-artifact reconciliation。

由于CLAIM和canonical native evidence artifact已经存在，不能简单删除CLAIM、换Run ID或直接再发模型请求。必须先设计并验收“CLAIM存在但实际HTTP请求为0”的恢复政策，再决定是否允许新的唯一请求槽。

本轮没有修改main、没有修改定时任务、没有真实订单、没有O全池、没有U模型调用、没有重写Run008 A0/A3。
