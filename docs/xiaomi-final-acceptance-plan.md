# 小米单股验收交接与最终测试要求

日期：2026-09-12。父检查点：`25ec5dd96a74ba38d395e637dd7451a0f2962c6e`。
本文件是本项目的阶段验收说明，配套 `xiaomi-acceptance-checkpoint.json`。不替代 AGENTS.md，不改变其他市场或美股模型规则。

## 当前判定

四步任务尚未全部验收通过。完整日线和新闻输入链路已有可复核成果；最新代码通过182项相关测试，但最近真实生成的报告人工复核失败，修复后尚未再生成并验收新报告。

- 行情：OpenBB/Yahoo历史70个共同交易日核对完成；后续腾讯与直接yfinance的126个共同交易日OHLC及成交量一致。完整交易日为2026-09-11。OpenBB报价未提供足以确认的交易时间戳，快照成交量比历史日线多600股，不能将快照混入完整日线计算。抓取时间不等于交易时间；复权本次区间吻合不等于已验证所有公司行动。
- 新闻：最近实际8条、5个出版机构，进入数据库和DSA上下文；FT RSS可读但小米命中0。URL和标题去重已做，跨语言事件聚合、完整发行人公告覆盖和未解决风险持续追踪未完成。OpenBB公司新闻已实测，但返回的行业/竞争对手背景不能冒充全面小米公司新闻。
- 报告：已查到至少3次产出模型报告的运行。早期34642291178、区域新闻轮34680709223及追加轮34688694878均有内容问题；不以Actions的success替代人工验收。
- 费用：最近2次原始usage可核，估算合计0.06713764元；早期用量口径不一致，整个任务实际费用未知。无新增订阅。当前这次报告与规划工作未触发新的DSA模型调用。

运行详情、行情事实及错误原文见 [已有验收记录](xiaomi-regional-model-acceptance.md)。原版main固定在 `1c2b7642f594714be9f0c4d73a08cc492ccd0813`；修复全部在独立分支。

## 第三阶段成果在本项目中的应用

已读取《模型审计与校准｜第三阶段》的接管报告、17:24真实运行报告、18:50工作流双轮自检，以及 `Stage3_Workflow_Upgrade_20260912.zip` 的执行规范与验证结果；本次独立核验其9项清单哈希全部相符。未重新运行其20份SEC数据处理，不将另一项目的软件或市场判定移植为港股有效性证据。

本次已经应用：按父提交和源哈希建立交接；按实际运行号去重列账；分开记录工程检查、新闻语义复核、模型报告批准和投资有效性；完整登记失败与未知项；先复用现有GitHub和确定性回放，不引入无关插件、通知、订阅或美股代码。配套JSON为历史状态快照，供后续读取，不是已部署的自动续作引擎。

下一轮实施要求：

1. 复用现有prepare/run验收入口，增加外部任务清单和断点状态，不创建平行主流水线。每批使用唯一run_id，记录父检查点、代码版本、输入哈希、抓取与公开时间；文件原子保存。同任务和相同证据可复用已完成检查，过期行情/新闻须重抓。重复或冲突运行不得再计一次成功。
2. 从真实逐源记录汇总成功、无相关结果、失败、过期、身份不符、隔离、重复及待处理数。不得通过删掉失败源或遗漏新闻压低分母。文章、原媒体集团、独立事件分别计数。
3. 将近期新闻与未解决风险账本分开。已知监管或当事方指控即使超过72小时，也只能转入持续风险，不得自动消失或被解释为“无利空”。保留最初公开时间、最近核查时间、原文链接、公司回应和状态不确定性。
4. 统一结构化执行依据；入场、止损、减仓动作、仓位与风险计算由同一组字段生成并确定性复算。不能只加措辞补丁。证据不足可输出观望/数据缺口，不能编造基本面或强制给买入方案。
5. 报告批准必须同时满足输入校验、程序检查、逐项语义复核和输出路径检查。API/Web/Agent绕过主pipeline的入口及失败后历史、渲染、通知行为均须覆盖或标明未验证；禁止把既有182项测试算作这些入口已全部通过。
6. 使用缓存反例先做免费回放；新增网络取数只为具体缺口；深度研究仅在用户明确请求或任务符合其路由时使用。模型调用按未来获准的次数与预算执行，发送前留账并禁止隐式重试。费用不清不能写成零。

第三阶段原项目的41.5%工程估计、NO_GO/UNCALIBRATED、美股Universe、市值门槛与调度任务均不适用于本港股任务。其提取器的续跑能力已在原项目离线验证；DSA的跨运行续跑、风险账本和新最终报告仍待下面的最终测试，不声称已经部署。

## 最终测试顺序和停止条件

固定顺序为：核对断点 → 收敛新闻与执行字段契约 → 真实反例及缺失/重复/跨窗风险测试 → 免费新数据验收 → 获准额度内一次模型调用 → 程序和语义双重复核 → 保存完整证据及判定。

建议下一条用户 /goal 授权最多2次真实模型HTTP请求，累计按调用前现价保守估算不超过1元人民币。第一次失败仅在找到具体原因、完成实质修复并通过无模型回放后允许第二次；禁止重复抽样直到碰巧通过。本条是未来授权建议，当前消息仅授权核对、出报告和规划，没有据此新增调用。

本轮验收采用既有“财务未核验则隔离”模式。允许通过的是明确披露财务缺口的行情新闻简报及其一致性，不代表完整基本面DSA已修复或策略盈利有效。如果决定恢复财务数字，须先逐字段核验原始披露、期间、币种、单位及是否估算；恢复失败时不得宣称完整功能通过。

任一强制门槛未过，保存NO_GO、已完成项、阻塞原因及下一步；模型预算耗尽后不再收费重试，但继续完成免费的诊断、代码修复和交接。通过时也只关闭本次小米单股范围。TOP3、双模型实盘比较、定时简报和其他股票池扫描另行验收，本轮不启用。

## 本次变更验证及回滚

仅更新验收文档和历史状态JSON，核对实际文件、工作流名称、运行号与源包哈希。未运行新的后端测试，未修改工作流、模型密钥或运行代码。状态清单为历史证据索引，不能直接用于自动生成下一次PASS。撤回本次三个文档文件的变更即可回滚，不需更改main或05模型工作流。本文无对应英文文件，面向本WORK中文交接。
