# 小米单股追加一次验收

授权日期：2026-09-12。用户在得知本轮两次模型请求已用完后，表示可以继续授权费用。本次按最小范围执行：已有DeepSeek再请求一次，单次预留0.40元；不新增订阅。原版main保持不动。

父提交：6bef9ff0e3699261cf22c8aacd61891be8cab173。前两次模型运行34693409713、34694414050，原始用量估算合计0.06851864元，实际单次扣款未知。最后免费测试34694901922通过221项。

保留prior_calls=2，新增ACCEPTANCE_MAX_CALLS=3（脚本默认仍为2），不清零历史次数。前次累计预留0.80元，本次后累计预留1.20元；等于既有申请阈值，超过则发送前拦截。0.40元是请求大小上界的保守预留，不能与实际账单混同。

先在05既有工作流进行免费回归及新鲜行情、公司新闻、公告目录预检，通过才请求模型一次。关闭备用模型与自动重试，仅允许本次push的第1次运行；手动重跑和dispatch不调用模型。保留来源标签在原始复核记录中。通过程序检查仍须逐项人工审阅，失败时不自动抽样。

本轮不启动TOP3、定时任务或其他股票。财务未核验继续隔离，缺失证据保持未知。结果、原始用量、回放、代码和费用另行记录；完成后移除push触发并关闭任务条件。状态：PREPARED，未发生新增请求。

变更仅影响验收脚本和该工作流，不影响日常用户配置。本文没有对应英文文件。回滚本次验收入口和脚本修改即可，main无须修改。


## 已执行结果

实际模型运行：[34697756860](https://github.com/Rupheal/daily_stock_analysis/actions/runs/34697756860)，代码c30dca17c18d9005542db4692e7ec133eae30600。125项相关测试通过，126个日线共同交易日通过，6篇核心公司文章来自4家出版机构。预检2026-09-12T13:56:44.882586+00:00。

仅一次新增HTTP请求，命中输入2816、未命中7446、输出5317 tokens。按当时Flash空闲单价估算0.02877032元；与此前两次合计0.09728896元。实际扣费未知，即时账户余额未改变不证明免费。

原工作流NO_GO来自两处误拦截：“尚不构成放量突破或缩量回踩确认”和“而非自上方缩量回踩”。修正否定范围后，保持真实原始输出不变，免费回放通过选定数值与来源检查；来源说明确定性生成，执行模式仍为watch。原始工作流失败记录不改写为成功。

人工复核仍保留限制：原文“均线系统仍发散向下”未准确区分MA20当日上升；K线段落“收盘略低于开盘价上方压力区”未定义压力区，不能核对；未来1-3日震荡概率没有校准依据。交付仅采用可复算事实和明确的风险证据状态，不签发原始预测与交易执行方案。最终工程/语义验收各自单列，不把软件回放通过写成策略有效。

追加额度已耗尽，05工作流关闭。继续运行07免费回归核验本次校验修复；不再调用模型。完整远端artifact下载由连接器取得引用，但本地下载返回403，交付包仅含日志提取证据；远端原件摘要不能称为本地独立验签。


## 免费复测与交付收尾

[34698138162](https://github.com/Rupheal/daily_stock_analysis/actions/runs/34698138162)：230项通过、7项警告、14.54秒。模型HTTP为0；新鲜日线与新闻预检也通过。最新预检2026-09-12T14:05:01.075106+00:00。修复代码14d46ddcee3d112e06e7eb977e6f0282402d6a85，模型入口已关闭。三页中文追加验收PDF与证据包交付，报告区别程序回放通过和原始自动报告人工未批准。
